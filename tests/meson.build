# subdir('plink')

executable('plink-client-test',
  files(
    'plink.cpp',
  ) + plink_client_files,
  dependencies : plink_client_deps,
)

executable('chub-client-test',
  files(
    'chub.cpp',
  ) + chub_client_files,
  dependencies : chub_client_deps,
)
